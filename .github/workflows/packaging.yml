# The purpose of this action is to build the contents of this repository into an SBOL package
name: packaging

# Controls when the action will run.
on:
  workflow_call:
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:   
  directories_and_excel:
    name: Check package structure and export from Excel files
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repository for building
        uses: actions/checkout@v3
      - name: directories_and_excel
        uses: mburridge96/igem-plant-parts/.github/actions/directories_and_excel@inherit_workflow
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}

  retrieve_parts:
    name: Retrieve missing parts
    needs: directories_and_excel
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repository for building
        uses: actions/checkout@v3
      - name: retrieve_parts
        uses: mburridge96/igem-plant-parts/.github/actions/retrieve_parts@inherit_workflow
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}

  build_packages:
    name: Build and validate final packages
    needs: retrieve_parts
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repository for building
        uses: actions/checkout@v3
      - name: build_packages
        uses: mburridge96/igem-plant-parts/.github/actions/build_packages@inherit_workflow
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
      

  build_distribution:
    name: Build and export distribution
    needs: build_packages
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repository for building
        uses: actions/checkout@v3
      - name: build_distribution
        uses: mburridge96/igem-plant-parts/.github/actions/build_distribution@inherit_workflow
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}